# Bugs (history)

Fixed bugs, archived from `BUGS.md` so the active list stays scannable. **Archaeological**: read only when consulted. When a bug is fixed, append its entry here rather than to the active file.

The bug breakout file at `bugs/<slug>.md` (when present) stays in place as the historical diagnosis record; the entry here is a brief description of the fix and the commit it landed in.

## Cross-reference resolution

`/nightshift:ready` does **not** parse this file for work; only the line-discipline check reads it. When a bug is fixed, every other `**Requires:**` line in `FEATURES.md` / `BUGS.md` that referenced it is edited at the same time to drop the now-satisfied reference (mirror of the `FEATURES.md` convention). The active `Requires:` lines therefore describe what is *currently* blocking; this file is purely archaeological.

## Entries

- **Release status check requires an unpublished candidate to claim publication.** The shared README status expression accepts both in-development and published-on-main declarations, while retaining manifest version equality, monotonic version checks and missing or mismatched status rejection. Regression fixtures cover candidate preparation, stale versions, both status forms and malformed declarations. Fixed locally during 3.0.11 preparation; publication remains separate.

- **Unattended Stop hook blocks yielding to a present user for a blocking decision.** `internal/runtime/hook.js` now permits the unattended yield when nothing is actionable, no worker is active and every remaining blocker is a `user-decision`, with a message that the run is paused on those decisions and that session closing remains due; `capability`, `resource` and `dependency` blockers keep resisting until closing is recorded, and a permitted yield leaves the revision and reminder count untouched. Hook fixtures cover the permitted state and each resisting state, and `internal/runtime/REFERENCE.md` and `internal/workflow.md` describe the pause before closing. Shipped in plugin 3.0.9.
- **Unattended Stop hook blocks yielding while a background dispatch runs.** The read-only runtime `wait` operation (`internal/runtime/wait.js`, exposed by `internal/runtime/cli.js`) observes a saved worker until it finishes, its runner process disappears, the run stops, the run deadline passes or a bounded timeout elapses, and returns the project-relative receipt path with an explicit reason; the reference, brief and handover skill direct the controller to await a background dispatch inside the turn with it, so the Stop hook keeps resisting a yield while a dispatch worker is active. Runtime fixtures cover timeout, mid-wait completion, a missing runner racing a committed completion, an `unverified` worker, a stopped run and the deadline. Shipped in plugin 3.0.9. At inbox triage on 2026-09-13, the user accepted observation 2 of `2026-09-12-probe-returns-full-state-and-foreground-wait-recurrence.md` as covered by this shipped change: FeatherPod-Private run prefix `216f01e8`, unattended on Claude with plugin 3.0.6, used a foreground `.tmp/nightshift/wait-dispatch.sh` poller after each of eight dispatches because yielding was blocked. The poller waited for non-empty stdout or stderr captures with an approximately 9.5 minute bound; the report described repeated controller effort, with no functional failure. This also recurred after run prefix `39735588` on 2026-09-11. No retest of those original runs is claimed; the report's separate probe-response and dispatch-receipt observations are not closed by this disposition.
- **Init-backlog flags non-document files that mention unmigrated `.claude` paths.** `Setup.referenceDecisions` in `internal/setup.js` now flags a file outside the Markdown, JSON, TOML and YAML set only on a mismatch: an undeclared file whose rewrite would change its bytes, or a declared active consumer whose rewrite is a no-op, which is the signal for a computed or unsupported path literal. An undeclared script that only mentions unmigrated `.claude/` paths needs no decision; the init-backlog skill text describes the rule and a setup fixture covers all three cases. Shipped in plugin 3.0.7.
- **Init-backlog translates every `.claude/` ignore rule into a `.nightshift/` twin.** `Setup.preservePolicies` in `internal/setup.js` now asks `git check-ignore --no-index --verbose` which root `.gitignore` lines actually match the migrated file sources and migrated legacy directories, translates only those into the "Nightshift migrated ignore rules" block, and precedes the block with a blank line when the existing content does not end with one; host-only rules such as `.claude/skills/` stay untranslated. A setup fixture mixes migrated and host-only rules and checks that a rerun stays idempotent. Shipped in plugin 3.0.7.
- **Published version surfaces can drift on release.** `tools/release-gate.js` compares the outgoing range with the published baseline and fails when bundled non-test paths under `skills`, `internal` or `hooks` or non-version manifest fields change without a version increase, when the manifests differ, when the version decreases, or when the README status line does not carry the manifest version. It runs as the repository pre-push hook (`git config core.hooksPath .githooks`) and in CI against the push or pull-request baseline, the packaging test asserts the README status matches the manifests, and the Codex handshake has read its version from the manifest since 3.0.2. Shipped in plugin 3.0.3.
- **Mixed backlog line endings abort init-backlog instead of being repaired.** Controlled backlog files with mixed LF and CRLF endings now produce an approved mechanical repair that uses the existing newline policy, asks for LF or CRLF when the policy is genuinely ambiguous, composes hard-wrap repair when needed, and retains byte-exact backup, recovery, and resume guarantees. Invalid UTF-8, bare carriage returns, and mixed endings outside the backlog surface remain blocking. Shipped in this commit (plugin 2.6.20).
- **Init-backlog rejects Windows forward-slash project roots as noncanonical.** Root validation now treats forward slashes as Windows separators only when their native-spelling replacement matches the exact canonical path, reservation returns that native `canonicalRoot`, and the skill carries it through the request record and remaining spool lifecycle. Other aliases remain rejected. Shipped in this commit (plugin 2.6.17).
- **Shared sync-gists skill names the wrong backlog command.** Corrected the canonical personal `sync-gists` skill's Nightshift suite note from `init-workflow` to `init-backlog`, so the documented command matches the repository and shipped plugin. Shipped in this commit.
- **Feature breakout dependency lines drift from the active index.** The index is now the sole dependency authority: `**Requires:**` holds in-backlog links only and a new optional `**External:**` line holds external primitives, every breakout file under `features/` and `bugs/` lost its dependency line, and `skills/ready/ready.js` reports bare text in Requires, links or none. in External, an empty label, and any dependency line found in a linked breakout file as structural errors carrying a remedy. Every prose surface (index headers, ready/exploring/handover/revise-docs/init-backlog skills, README, AGENTS.md) describes the two-line convention. Shipped 2026-08-22 in the range 17d24cd..HEAD (plugin 2.6.0); diagnosis stays in [`bugs/feature-breakout-dependency-drift.md`](bugs/feature-breakout-dependency-drift.md).
- **Plan refreshed stamps are rejected by the provenance grammar.** `REFRESHED_PROVENANCE` in `skills/spec-agreement/spec-agreement.js` accepted only `revise-spec refreshed` lines while the graduated grammar already accepted both loops, so a plan edited after its graduated stamp could not carry the `revise-plan refreshed` stamp that handover's post-stamp-edits rule prescribes. The regex now accepts `revise-(?:spec|plan) refreshed`, and a spec-agreement test writes a plan refreshed stamp through `writeProvenanceStamp` and reads it back, including the applied-retry path. Shipped 2026-08-22 (plugin 2.6.1).

### Self-hosting implementation bypasses the Nightshift lifecycle

Observed in this repository on 2026-09-12 while implementing the ready skill's Exploring presentation change, committed as 1d1812d. The controller used direct fresh-context reviewer and skeptic agents, cumulative reassessment after repairs and native-host presentation probes, but did not invoke revise-code or operate the durable Nightshift lifecycle. Subsequent user questions established that revise-docs and revise-lore were also omitted: documentation and verification evidence were written directly, without the required documentation reconciliation and session retrospective workflows. Repository instructions require the v3 independent review and repair process for Nightshift development, followed by documentation reconciliation and the session retrospective before follow-up triage. The controller reported independent review and documentation updates without distinguishing that evidence from completion of the required self-hosting process; the omissions became explicit only when the user asked which skills had run.

Investigate and repair workflow selection and closing for repository implementation so the self-hosting requirement activates the required Nightshift machinery through revise-code, revise-docs and revise-lore, with documentation reconciliation preceding the session retrospective and follow-up triage. Preserve the explicit direct-agent exceptions for user-requested review and review-loop; ordinary implementation is not such an exception. Verify the complete sequence with installed-host evidence and make completion reporting identify the process actually used and every required lifecycle stage still missing. Existing independent reviews, probes and direct documentation edits remain evidence, but must not be represented as retroactive lifecycle completion. The observation is captured in this conversation and the limited presentation evidence is preserved in [the ready verification report](reports/ready-exploring-presentation-20260912.md).

Candidate repair in 3.0.12 explicitly activates the runtime for agreed self-hosting implementation and connects revise-code, revise-docs and revise-lore to their governing stages. The [verification report](reports/self-hosting-lifecycle-activation-20260913.md) preserves native activation and closing/routing observations, synthetic-fixture qualifications, shell limitations and budget history. Run 3a98dc79-b675-43c6-8236-1b997c443680 owns cumulative acceptance and closing. Publication is separate, and earlier omitted stages are not claimed as retroactively completed.

### Installed plugin-cache replacement disrupts active runs

Fixed in version 3.1.0. Complete manifest-verified retained releases, a stable host-level launcher, exact session/run/worker identities, pre-work rediscovery and guarded entry points survive cache replacement. Hashed text has LF attributes and autocrlf checkout coverage. All twenty acceptance items on each supported host have observations, with the host/model and source-removal qualifications preserved in the [acceptance report](reports/retained-plugin-releases-20260914.md). Complete clean cumulative Astra assessment `12329139-2ee6-4a44-bfd2-5e56b77161bd` independently verified the implementation and native evidence. The [governing design](specs/retained-plugin-releases.md) and durable run retain the full acceptance and assurance record.

A further incident occurred on 2026-09-13 in this repository, Codex session `01a09c60-dd6e-7d42-b8a0-7333fd211d92`: Ready read its 3.0.11 skill instructions, but the subsequent bundled ready.js invocation failed with MODULE_NOT_FOUND. Inspection found only the 3.0.12 cache, including ready.js; the old directory had disappeared. The repository parser supplied the report, so this was a stale installed-root reference rather than a missing script in the new release. The user requested a durable fix, approved retained releases and host-level setup, required a shipped completeness manifest, and permitted selecting the current release if initial capture fails before any work begins. Governing design: [Retained Nightshift releases](specs/retained-plugin-releases.md).

Reported twice from `C:/Git/FeatherPod-Private`. On 2026-09-12, run `655d10db-0f93-436c-8b41-86a882761ced` launched review `bf2af192-0d39-4b29-b09d-6274f7663bb9` through the Codex plugin cache at 3.0.8. The review completed, but that directory disappeared before receipt import and only 3.0.9 remained; the previously working `internal/runtime/cli.js` invocation failed with `MODULE_NOT_FOUND` while the controller session remained active. The 3.0.9 runtime subsequently read revision 13 and imported the intact 3.0.8 receipt at revision 14 without source or receipt edits. On 2026-09-13, run `288d395f-2cb9-4d7f-bd8c-f4ff78d8c0c2` encountered the same resource loss during cumulative review `0e33c0aa-7e67-4512-9038-b31c95509d08`: reading the installed 3.0.10 runtime failed with "The system cannot find the path specified", and the cache contained only 3.0.11. Versions had changed repeatedly during that run. A scratch wrapper resolving an existing runtime on each call allowed subsequent wait/status operations, and the failed read was retried against 3.0.11; the report does not establish reviewer completion or absence of impact. This is separate from the premature-yield and manual-goal-resume incident.

A last incident on the final release without the fix was reported on 2026-09-14 from `C:/Git/aoa-registration-of-arrival`, run `9638e874-7f43-4dd3-a5c6-7700fa646ee0`, and triaged here on 2026-09-18. During an active handover and cumulative code review started on 3.0.12, a routine status call failed with `MODULE_NOT_FOUND` for the 3.0.12 `internal/runtime/cli.js` in the Codex plugin cache, which then contained only 3.1.0; the process that removed the old directory was not observed. The review process was still running and the project state database was intact. A retained Claude cache copy of 3.0.12 had an identical runtime reference document, so the controller copied that version into the run's ignored artifacts and pointed its request helper there; status then recovered revision 181 and the same active worker `b414635c-5460-47f8-812c-5a66a8bd11c9`, with no canonical project or Nightshift source files changed. The run began before retained releases existed, so this does not bear on whether the 3.1.0 fix holds. Original inbox report: `2026-09-14-active-handover-cache-pruned-aoa.md`.

Investigate resource availability and safe recovery when an installed plugin cache is replaced during an active run. Establish how the controller and surviving workers retain or recover compatible workflow/runtime resources, collect existing receipts and continue durable state without assuming that selecting the newest installed version is safe. Preserve review evidence and report a capability blocker when compatible recovery cannot be established. Verify cache removal during an active review and between review completion and receipt import, covering compatible recovery and unavailable or incompatible resources. Original inbox reports: `2026-09-12-runtime-path-removed-during-featherpod-handover.md` and `2026-09-13-featherpod-cache-replaced-during-review.md`.

### Handover omits the morning report

Observed during retained-release delivery in this repository, run `2a89bde5-9c0c-4ba5-b67f-dadc545bfcc4`, Codex session `01a09c60-dd6e-7d42-b8a0-7333fd211d92`. The user explicitly handed over settled work. The controller performed independent code assessment, documentation reconciliation and a recorded retrospective, but ended with brief completion/publication summaries and a technical acceptance report instead of the expected morning report. The user had to ask whether a morning report existed and whether revise-code, revise-docs and revise-lore had run. The documentation pass had also missed stale README acceptance wording, repaired only during publication review. The user identified the missing morning report as a separate bug from the run remaining attended. This entry promotes and consolidates the former quick win "Morning report ends by starting follow-up triage"; the earlier incidents remain below.

Observed in this repository on 2026-09-11. An unattended run finished with two follow-ups preserved as pending user decisions. When the user returned, the controller's closing report listed them as awaiting a decision and stopped; the user had to ask for them, and they were then presented one at a time with a host question each, as the triage rule prescribes. The report and the triage are one hand-off: once the user is back, the pending decisions are the next thing to settle.

Related incident on 2026-09-12 in FeatherPod-Private, plugin 3.0.9, run `655d10db-0f93-436c-8b41-86a882761ced`: delivery, independent review and documentation were complete, but the controller asked about an optional follow-up before delivering the morning report, paused at retrospective and eventually marked the native goal blocked. At revision 52 there were no active workers or task blockers; the only remaining decisions concerned episode deletion ownership cleanup and permanent history/editing integration coverage. The user directed that these decisions follow the morning report. The runtime accepted retrospective and deferred-triage evidence at revisions 53 and 54, then completion with pending follow-ups at revision 55; revision 56 registered the incident. The controller cited the brief's instruction to ask first and close after answers, alongside the handover pause rule. This establishes the reporting failure, while its cause across controller interpretation, wording and runtime behavior remains to be investigated. Authoritative history remains in that project's `.nightshift/runs/state.sqlite`; supporting evidence is in `.tmp/quick-wins-closing-retrospective.json` and `.tmp/quick-wins-closing-triage.json`. Original inbox report: `2026-09-12-featherpod-pause-before-morning-report.md`.

Related incident on 2026-09-13 in this repository, self-hosting activation run `3a98dc79-b675-43c6-8236-1b997c443680`: the controller reported completion, checks, local commits and that estimation/overshoot follow-ups were tracked, but omitted the open follow-up scope and the retrospective's other observations. The user had to ask whether anything remained unhandled and what revise-lore found. Only then did the controller distinguish the open budget-workflow repair from scratch fixes already tested, and identify omitted review evidence inputs and a provider structured-output whitespace failure as recovered incidents recorded only in retrospective evidence. The user explicitly objected to having to pull this information out of the controller. The run had no pending triage decisions; that did not make its open tracked work or retrospective outcomes irrelevant to the morning report.

Related incident on 2026-09-14 to 15 in `C:/Git/aoa-registration-of-arrival`, run `9638e874-7f43-4dd3-a5c6-7700fa646ee0`, started on plugin 3.0.12 while the installed Codex cache moved to 3.1.0 during the run: the final report said that an existing Release analyzer error was untouched and its tracking decision remained pending, but omitted the error identifier, location, failed condition, impact and the actual pending choice. Those details (MA0051 at `AoA.WebApi/Program.cs:60`, a 66-line method over a 60-line limit, Release compilation failing while Debug passed, C# changes excluded by the user, and the choice between tracking the issue separately or leaving it in the handover report for a colleague) had appeared only in progress output and an unanswered asynchronous question during the autonomous run. The user had to ask what the follow-up was, and stated that the controller must not assume progress logs were read during an autonomous run. The controller then added a global reporting rule, which the user had undone because Nightshift behavior is to be fixed inside Nightshift. The omission and the corrections are confirmed by that conversation; whether the cause is workflow guidance, enforcement or controller execution is unverified. Original inbox report: `2026-09-15-autonomous-report-followup-context.md`.

Preserve a user-facing morning-report obligation after explicit handover through interruptions, resumption and completion. Track report delivery independently of the attended-to-unattended transition: a failed mode transition does not cancel the reporting obligation, and a correct mode flag does not prove the report was delivered. A technical acceptance artifact, saved retrospective or short commit/push confirmation is not a substitute. Identify which required revise workflows actually ran and any deficiencies or later recovery, so a completed stage label cannot conceal missing work. Verify the complete readout after a resumed handover and after a mode-transition failure, as well as the returning-user cases below.

Reconcile `skills/handover/SKILL.md` and the brief's "Close and report" section so optional follow-up decisions do not delay the morning report or block an otherwise completed delivery. Preserve those decisions through closing when the user is unavailable. The report delivered to a returning user ends by presenting the first pending follow-up as a host question, with its concrete context, recommendation and the effect of each choice, then the next after each answer, rather than listing them for the user to request. Distinguish optional follow-ups from unresolved delivery blockers, which remain reported as blockers. Surface material retrospective outcomes and follow-up dispositions proactively, including when no decision is pending: distinguish implemented repairs, open tracked work, recovered observations without a durable follow-up, and instruction proposals or the reason none was worthwhile. A statement that triage is complete or follow-ups are tracked must not substitute for that readout. The report stands on its own: it must not assume the user read progress output, the continuous log or an unanswered question raised during the autonomous run, so every unresolved item carries enough context there (what it is, where, what failed, its impact and the pending choice) to be understood and decided without them. Verify installed-host behavior both when the user is absent and when they return, including completed delivery with optional decisions, completed delivery with no pending decision but material retrospective outcomes, and delivery with a genuine blocker. Shipped text changes model-owned behavior, so it rides with the next version increase.

Fixed in plugin 3.2.0 under [the governing spec](specs/handover-transition-and-morning-report.md). A handed-over run now owes a morning report as a recorded closing stage between the retrospective and triage: `report` stores a hash-bound file under `.nightshift/runs/reports/`, `triage` and `complete` each refuse without it, and `report-delivered` records delivery from the user's actual reply, never gating completion. The report and the follow-up triage are one hand-off: the report ends with the first pending follow-up as a question, one reply records both delivery and that decision, and the owning session's SessionStart carries a notice while the report is undelivered or a follow-up is pending, including on a completed run. The brief narrows "ask first and close after the answers" to task blockers, and a run without a handover ends by listing its loose ends. Observed on installed Claude Code and Codex; the qualifications, including a Codex final-message deviation repaired in shipped text, are in [the acceptance report](reports/handover-transition-and-morning-report-20260919.md).

### Handover leaves existing runs in attended mode

Observed during retained-release delivery in this repository, run `2a89bde5-9c0c-4ba5-b67f-dadc545bfcc4`, Codex session `01a09c60-dd6e-7d42-b8a0-7333fd211d92`. The run began attended. The user explicitly handed over the already-agreed work, but the saved mode remained attended through completion. The runtime accepts mode at creation and provides no supported transition for an existing run. Native goal continuation carried work forward, but it did not change the saved mode or establish that the main run used unattended Stop enforcement. This is distinct from the separately successful unattended acceptance fixtures. The user identified the stuck mode as a bug after delivery.

Recurred on 2026-09-15 in run `c675a074-6431-46e2-85b7-e3b8616e9220`, an attended development run of the automatic-preparation change that the user handed over during its Codex phase: the saved mode again remained attended through completion, a native persistent goal carried the queue, and no retained Stop-hook protection could be claimed. Second observed instance. The runtime accepts mode only at creation, so the defect is not specific to either host, although both observed instances were Codex-hosted.

Provide an authenticated handover transition for an existing owned run that verifies native continuation prerequisites and switches to unattended operation while preserving its identity, queue, workers, accepted commitments, valid evidence, limits and publication authority. Define interruption and retry behavior without recreating the run or directly editing saved state. A failed prerequisite must preserve the existing state and report the actual limitation rather than claim a completed transition. Preserve agreement already established in discussion or a governing spec; handover must not require another readback approval unless consequential requirements changed or decisions remain unresolved. Verify new and existing runs, active workers, repeated handover, failed continuation prerequisites and recovery on both supported hosts.

Fixed in plugin 3.2.0 under [the governing spec](specs/handover-transition-and-morning-report.md). A new `handover` lifecycle action hands an existing owned run over in place in one write: with a verified mechanism carried in the same request it stores the mechanism and switches the mode to unattended, and without one it records the handover and leaves the run attended so the report obligation survives a failed transition. A repeat keeps the original record, can upgrade and never downgrades, and nothing else in the run changes. When continuation cannot be verified a new run starts attended, because a run created unattended refuses all work until verification. Refused admission records nothing and the handover is not accepted. In-place handover with an active registered worker and a repeated handover were observed on both installed hosts; the wait while a runtime dispatch holds the lease has deterministic coverage only, as [the acceptance report](reports/handover-transition-and-morning-report-20260919.md) records.
