# Quick wins (history)

Implemented quick wins, archived from `QUICK_WINS.md` so the active backlog stays scannable. **Archaeological**: loaded on demand. When a quick win lands, append its entry here rather than to the active file.

Entries appear in the order they shipped. Write each with enough context to recover the reasoning from the entry alone: investigation findings, reverted approaches, benchmarks, the commit or scope it landed in. Negative-knowledge findings (approaches attempted and reverted, with the reason) are the most valuable content here for preventing re-attempts; consider promoting those into the relevant `.nightshift/patterns/<slug>.md` Cautionary tales section when touching the pattern doc, leaving a one-line redirect here if cross-referenced.

## Cross-reference resolution

`/nightshift:ready` does **not** parse this file for work; it reads it only for the line-discipline check and the link checks, which verify links into it and count its links as keeping the records they name reachable. When a quick win lands, every other `**Requires:**` line in `FEATURES.md` / `BUGS.md` that referenced it is edited at the same time to drop the now-satisfied reference. The active `Requires:` lines therefore describe what is *currently* blocking. This file is purely archaeological; read it when you want to know what already shipped or to mine negative-knowledge findings, not to resolve dependencies.

## Entries

- **Scaffold a run-`/nightshift:ready`-after-adding-an-entry instruction in `/init-backlog`** (`commands/init-backlog.md`): the four index templates now instruct the author to run `/nightshift:ready` after adding a new entry (or per-item breakout file) to confirm it parses and its `**Requires:**` line resolves against the real grammar in `skills/ready/ready.js`. Scoped per index to what the parser actually validates: FEATURES/BUGS resolve `Requires:` links and flag structural errors; QUICK_WINS surfaces prose-only-section notices; PATTERNS.md is not parsed (it is a registry), so its note directs a breakout-file link check plus a whole-session sanity pass. A matching `## Concept checklists` item per index makes the idempotent `init-backlog` re-run detect the missing concept on already-scaffolded projects and propose a targeted patch. Validated by a fresh-context acceptance re-run of `init-backlog` against this repo: exactly the four stale indexes were targeted-patched and every other target skipped. Shipped in this commit.
- **Pin the revise dedup judge to sonnet instead of inheriting the artifact model pin** (`skills/revise/revise-round.workflow.js`): the Workflow dedup judge now passes a fixed `sonnet` model override at `effort: 'low'`, while reviewers and skeptics continue using the artifact profile model. A regression case exercises an `opus` profile and verifies all three outbound model choices. Plugin version 2.4.1. Shipped in this commit.
- **Decompose `extractEntries` and `analyze` in `skills/ready/ready.js` into named helpers**: entry extraction now delegates section state, heading and bullet construction, and prose-only notices. Analysis now composes index parsing, entry metadata, cycle detection, quick-win and draft collection, tracked-entry classification, breakout targets, and cycle errors. All 48 fixture cases preserve the existing JSON contract. This structure-only change does not require another plugin version increase. Shipped in this commit.
- **Require a clean LGTM to certify a fingerprint; a refuted-findings round no longer suffices** (`skills/revise/SKILL.md`): only a clean conclusion with a concrete verification note now certifies a dimension. Refuted-only and acknowledgement-only rounds stay active for another fresh pass, with their reasoned acknowledgements carried into the next payload and the 30-round cap retained as the livelock backstop. The holistic verifier follows the same clean-pass rule; only a current verifier round that applies no fix and creates an authoritative deferred follow-up retains the stamp exception because another pass cannot resolve accepted debt. README's two convergence summaries and the authoritative wave-lifecycle design record mirror the rule. Plugin version 2.4.4. Shipped in this commit.
- **Restructure the Manual Agent path session-reconciliation paragraph into a labeled case list** (`skills/revise/SKILL.md`): the single ~4900-character crash-recovery paragraph is now an ordered list whose labels expose each state precondition and whose bodies retain the existing result-then-state replacement actions, repair-counter handling, stale-session behavior, and best-effort guarantees. Plugin version 2.4.5. Shipped in this commit.
- **Do not raise the shift-start confirm for designed provisional live-claims plus a mid-flight implementation resume** (`skills/handover/SKILL.md`): the clean-detection ladder paragraph now names two non-flags, alone or together: a `(live-claim: provisional)` marker whose provisional status is a designed outcome (probe gated behind a cutover or release precondition recorded per the spec-gate rules), and a same-session mid-flight implementation resume; with only these present the run proceeds straight to building the queue, keeping the confirm for real ambiguity, artifact-selection doubt, validation findings, and drift. Step 9 narrows to match: a designed carry-forward provisional marker surfaces as a note like `probed` and `deferred` ones. The user ruling of 2026-08-11 recorded in the retired backlog entry is the design source. Plugin version 2.5.3. Shipped in this commit.
- **Replace handover's TaskCreate queue with a durable scratch-file queue** (`skills/handover/SKILL.md`): the flat step queue is now a scratch file at `.tmp/handover-queue.md`, beside `.tmp/handover-followups.md`, rather than a host task tool. Claude Code no longer exposes a TaskCreate tool, so the previous instruction could not execute as written and every session improvised. The new paragraph mirrors the follow-ups scratch rules: attributable header naming spec and target scope, one line per queued step carrying step number, name, and completion mark, whole-contents atomic replacement on update, dead-state overwrite versus same-artifact resume at shift-start, creation after the confirm line, and deletion only after step 12's whole tail alongside the follow-ups file. The stop-authority sentence changed from "until the handover queue is empty" to "until every step in the handover queue is marked completed", since a file with completion marks is never emptied. A host offering a native task list may mirror the queue; the scratch file stays the authority. Two design records were reconciled in the same change because both enumerated state this falsifies: `features/durable-run-identity-concurrency.md` (three sites asserting the scratch set is exactly the current flat files and that no new scratch file is introduced) and `features/agent-host-agnostic-nightshift.md` (the host-coupling bullet naming `TaskCreate`, plus its adapter-contract bullet narrowed from required task-queue creation to optional queue mirroring, matching the slice's own "durable handover state, rather than a host task list, becomes the stop authority" sentence). Also corrected an unrelated stale bullet in that same host-coupling inventory: the seven-commands-only gap was closed by the universal-skill MVP shipped 2026-08-18 in the 2.5.0 batch. The topology test's ordered-token pin moved from `build a single flat TaskCreate queue` to the new wording, and a second test pins the queue lifecycle contract itself. Two decisions are worth recovering from this entry alone. First, the queued step line is pinned to `- [ ] <step number>. <step name>` with `- [x]` as the sole completion mark, because a resuming session, possibly on another host, must recognize marks it did not write; left unpinned, each session invents its own convention and one session's unmarked step is another's marked one. Second, where the stage ladder's detected entry step disagrees with the queue's first unmarked step, the queue wins and the disagreement is raised as a confirm-first flag. An earlier-of-the-two-wins rule was drafted first and rejected during review: the ladder's entry steps top out at 5 and its only later rung is a same-session no-op, so earlier-wins would have discarded the queue on every cross-session resume past step 5 and re-run the entire late-stage tail, defeating the durable queue exactly where it was meant to help. Marks are trustworthy for this because a step is marked only after it completes, so the worst case is one redone step. Plugin version 2.5.5. Shipped in this commit.
- **Move generic release and conformance assertions out of the agreement controller suite** (`tests/release-surface.test.js`, new): five generic tests left `skills/spec-agreement/spec-agreement.test.js` (CI conformance, the repository command-list contract and its two callers, the suite-count pin, and the release-version assertion), and the mixed `README and AGENTS describe the ten-skill current-session agreement surface` test was split so the public-skill surface count moved while the agreement-contract assertions stayed. The suite went 119 tests to 114; the orphaned `documentedRepositoryTestCommands` and `assertRepositoryCommandList` helpers moved with their callers, while `extractSection` stayed because three other tests still use it across four call sites. Two decisions worth recovering. First, the relocation deliberately creates an eighth suite rather than folding into an existing one, which cascades into `AGENTS.md`, README's Development block, `ci.yml`, and the suite-count pin itself; the cascade is a one-time cost that the relocation exists to stop recurring, and the alternative (fold into `universal-skill-topology`) was rejected because that suite is about skill topology, not release surface. Second, the relocated version assertion was redesigned rather than moved verbatim, which is a deviation from the entry's "relocate" wording and not covered by the four boundaries agreed beforehand. It now checks the version's three-part shape and that `marketplace.json`'s description matches `plugin.json`'s. The old `assert.equal(manifest.version, '2.5.6')` had inverted value: it fired only when someone bumped the version, the correct action, and stayed silent when behavior shipped without a bump, the only thing the versioning convention actually cares about. It also could not see a malformed version or manifest drift. Nothing before or after this change detects the convention's real concern, since no test correlates a diff's file list against a version increase; that gap was closed on 2026-08-23 by **Detect shipped behavior changes that carry no version increase** (below). The entry's original three-site tally was correct and a first re-derivation wrongly called it stale: at the pre-change commit the suite held exactly three literal `seven` tokens, the test name plus the sentence literal and the assertion message. The confusion came from conflating that pin with `documentedRepositoryTestCommands`, which listed two commands and was never a count site. The new suite pins the CI command set both ways, so a suite added to CI but not declared, or declared but not run, both fail. A mutation probe confirmed every invariant bites, including a renamed skill row that an earlier count-only assertion let through. Tests, CI configuration, and repository guidance are version-exempt, so this shipped with no version increase at 2.5.6. Shipped in this commit.
- **Condition the missing-breakout notice tail on the entry's own classification** (`skills/ready/ready.js`): `breakoutReadNotice` used to append "(its Requires line still resolves normally)" to every non-draft missing-file notice, including entries whose own classification had ended in a structural error (every structural path reaches the notice, since `addBreakoutTarget` runs regardless of outcome). `classifyTrackedEntries` now derives a three-valued outcome from the entry's own `classifyTrackedEntry` call, as a length delta on `structuralErrors` around that single call rather than a title scan (two indexes can share a title): `structural` when the call filed an error under the entry title or a bracketed slice title, else `cycle` for a dependency-cycle member, else resolved. The outcome rides on the `breakoutTargets` record as `outcome`, carried by presence like `draft` and omitted when resolved, so the byte-stability assertion and hand-built recs are unchanged; `MISSING_BREAKOUT_TAILS` maps it to the tail. `structural` wins over `cycle` so the result holds in either landing order with the sibling fix below. Fixtures: a non-sliced structural entry, a well-formed and a malformed cycle member, a sliced entry mixing a ready and a structural slice, an end-to-end notice check for the structural and cycle tails, and the omitted-field pin. Shipped 2026-08-22 in the 2.6.2 batch.
- **Validate the External line of a dependency-cycle member** (`skills/ready/ready.js`): `classifyUnit` returned on `unit.excluded` before any grammar validation, so a malformed `**External:**` line on a cycle member (and a malformed inline `**Requires:**` on a continuation slice of a sliced member, a line the cycle error never owns because `collectEntryEdges` reads only the top-level line) surfaced only after the cycle was fixed. Whole-entry exclusion now suppresses classification, not grammar validation: the order is missing-Requires return, Requires loop, External loop, the single joined structural push (Requires first, so non-excluded reports are byte-identical), exclusion return, then classification. An excluded unit files its grammar problems under its own title and nothing else; a well-formed External on a cycle member still yields no External classification (the pre-existing pin). Fixtures: a cycle with a link in External on one member, a sliced cycle member whose continuation slice carries a bare-text inline Requires plus a link in External (one joined error, no Blocked entry), and a no-Requires entry with `External: none.` reporting only the missing-line error. Shipped 2026-08-22 in the 2.6.2 batch.
- **Detect shipped behavior changes that carry no version increase** (`tests/release-gate.js`, new; `tests/release-surface.test.js`): the release surface suite now gates the unpushed range on the versioning convention. A pure helper module owns the decisions: `classifyShippedBehavior` resolves the AGENTS.md shipped-behavior list to paths (`skills/**` and `internal/**` minus `*.test.js` and `fixtures/` directories, `hooks/**`, and `.claude-plugin/plugin.json` only when the manifest at the range base and at HEAD differ in a field other than `version`, compared with `node:util` `isDeepStrictEqual`), `assessVersionSequence` checks that the version at HEAD is strictly greater than at the base with no intermediate decrease (a malformed version fails closed through `parseSemver`), `resolveUnpushedRange` takes the merge-base with `@{upstream}`, else `origin/main`, else returns a skip notice, and `evaluateReleaseGate` composes them into a verdict with a reason string. Five suite cases pin each helper with fixed samples (so a predicate that matches nothing or a skip that always fires cannot stay silently green), pin the AGENTS.md shipped-behavior sentence verbatim with `countExact`, and run the live gate over real git (`git diff --no-renames --name-only`, `git rev-list --topo-order --reverse`, one `git show` per revision), reporting a `t.diagnostic` skip when no range resolves. Three decisions worth recovering. First, "exactly one monotonic version increase" resolves as a range-wide property: it excludes zero and forbids an intermediate decrease, and a range holding two bump commits passes; counting version-changing commits was rejected. Second, `--no-renames` is deliberate: default rename detection reports only the destination path, so a skill moved out of the shipped tree would otherwise classify as no shipped change. Third, only committed state is read; uncommitted edits are outside the convention, which scopes itself to unpushed batches, and in CI the gate sees an empty range on push or skips on a shallow pull-request checkout, so the local pre-push suite run was the enforcement venue until the CI checkout fetched full history (the pull-request entry below). Verified end to end in a scratch clone: a skill edit without a bump fails with `shipped behavior changed (skills/ready/SKILL.md) without a version increase`, the same edit with a bump passes, and a docs-only range passes. Tests are version-exempt, so this shipped with no version increase at 2.6.2. Shipped 2026-08-23.
- **Name the git failure in the version-increase gate's skip notice** (`tests/release-gate.js`, `tests/release-surface.test.js`): `resolveUnpushedRange` now retains the last error caught across its two `merge-base` probes and appends its message (whitespace-collapsed) to the skip notice as `(last git error: ...)`, so a missing git binary or a corrupt object store reads differently from a missing ref; a probe that throws nothing leaves the bare notice. Skip-with-notice stays the behavior and the `version-increase check skipped` prefix is unchanged. Shipped 2026-08-23 in 0870656.
- **Let the version-increase gate fire on pull requests in CI** (`.github/workflows/ci.yml`, `tests/release-surface.test.js`): the checkout step now carries `fetch-depth: 0`, so a pull-request checkout has `refs/remotes/origin/main` and `resolveUnpushedRange`'s existing `origin/main` fallback resolves the base branch tip from the detached merge ref; the live gate then evaluates the pull-request range instead of taking its skip-with-notice pass. The CI conformance test that already reads `ci.yml` pins the three-line checkout block, so a later workflow edit that drops the input fails the suite instead of regressing the gate silently. Decisions worth recovering: the base ref is `origin/main` via full history, not `GITHUB_BASE_REF` (the resolver iterates a fixed ref list and reading the environment would have needed a code edit the local-behavior-unchanged rule constrained; `origin/main` is the only remote branch); a push to `main` stays a vacuous pass because HEAD is `origin/main` and the range is empty at any depth; a run where no base ref resolves keeps the skip-with-notice pass; `workflow_dispatch` runs are not a target and whichever probe resolves there is accepted. Known and accepted: the exact-block pin misreports a benign `actions/checkout` version bump; when `main` bumps the version after a branch point the gate reports a decrease rather than a missing increase (still red, never a false pass); a pull request onto a non-main base resolves against `origin/main`. Verification: after the workflow edit ships, open one pull request and confirm the CI log of `tests/release-surface.test.js` carries no `version-increase check skipped` diagnostic, so the gate evaluated a non-empty range (live-claim: deferred 2026-08-23, no pull-request workflow in use; probe on the first pull request if one ever opens). Shipped 2026-08-23 in a3231ca.
- **Number the recommendations in the ready report** (`skills/ready/SKILL.md`): step 3 now renders the closing recommendations as an ordered list (1., 2., 3.), so a bare numeric reply selects one unambiguously; captured and shipped 2026-08-23 after a "3" reply had to be matched against unnumbered bullets. Shipped in the same batch as the spec-agreement closed-shapes documentation.
- **Stop committing implementation plans.** `.nightshift/plans/` is now always ignored, tracked-plan handling is removed from the handover and revise contracts, and implementation plans remain ephemeral while code and Git history provide the durable record. Shipped 2026-08-30 in the 2.6.14 batch (implementation commit f8b2fff).

- **Tell implementation subagents where their scratch files go.** Handover now requires the repository-root-only `/.tmp/` rule and effective `.superpowers/` coverage without editing user-owned policy, migrates its queue to protocol version 2 with one persisted full `implementationAuditBase`, audits staged and committed root scratch paths at every Git boundary, and gives every original, repair, replacement, or replay dispatch one fresh exclusively created task-and-dispatch-scoped directory under `.tmp/`. Git and non-Git branches share allocation while only Git performs policy, index, history, and untracked checks. Completed and interrupted directories remain in place; cleanup and post-dispatch physical-identity enforcement stay deferred to Night Manager. The future durable queue binding must migrate `resumeQueue`, `bindImplementationAuditBase`, and `advanceQueue` together without changing the field lifecycle. Plugin version 2.6.21. Shipped in this batch.

- **Make the governing spec the durable record that a provisional live-claim is a designed carry-forward.** The clean-detection non-flag for designed `(live-claim: provisional)` markers in `skills/handover/SKILL.md` relies on the probe precondition being recorded per the spec-gate rules, but that record is the morning-report follow-up item in `.tmp/handover-followups.md`, run scratch that survives a crash for the morning report's sake yet that no artifact-reading step consults: the validation agent reads the governing spec, so a fresh-session resume after a crash before the morning report has no on-artifact evidence that the marker is designed, and detection fails closed into the confirm the 2026-08-11 ruling meant to skip. Preferred shape: record the precondition on the spec at the marker site by extending the marker grammar in `internal/revise/spec.md` item 6 with `(live-claim: provisional, awaiting <precondition>)` (chosen over an inline prose precondition by user ruling 2026-09-02, because the failure mode is a detection that cannot see designed-ness on the artifact and a literal form is what downstream tooling can read), let the Validate-before-proceeding step read designed-ness from the artifact, and derive the morning-report item from that record instead of making the report the sole record. Migration: the implementation sweeps every marker already in the tree under the definition below, found mechanically over the governing-spec surface, `.nightshift/features/`, `.nightshift/bugs/`, `.nightshift/patterns/`, and the four indexes `QUICK_WINS.md`, `FEATURES.md`, `BUGS.md`, and `PATTERNS.md` (the history archives are not governing specs and the hash-pinned trees under `tests/fixtures/` are frozen, so both stay untouched), matching every occurrence of the substring `(live-claim: provisional` outside a fenced code block, whether bare or inside a backtick code span and whatever follows the closing parenthesis, except inside the grammar-owning sites enumerated in the clarification below; the markers that predicate matched at the time of writing, all of them rewrite candidates, are the two probe bullets in `.nightshift/features/wave-lifecycle.md`, the verification paragraph in `.nightshift/features/deterministic-init-backlog.md`, and the backticked claims in `.nightshift/features/calibrate-first-draft-rigor.md` and `.nightshift/features/second-opinion-gates.md`, and the seven bare markers in `.nightshift/features/operating-context-at-shift-start.md` (five for that feature, two for this entry's classification) are exempt because that batch-mate probes them in its own verification step before that feature lands, so they are not carry-forwards, while `probed` and `deferred` markers are untouched because only `provisional` gains the clause); a marker carried forward under the spec gate's release or cutover rule is rewritten to the `awaiting <precondition>` form naming its recorded precondition, and a marker with no recorded precondition stays bare and reads as undesigned, which is the intended semantics rather than a regression. The `awaiting` clause qualifies only the `provisional` state: a confirming probe or a knowing deferral rewrites the whole marker to the existing `probed <date>` or `deferred <date>` form and the clause drops. Every shipped consumer that matches the bare literal is swept to match any marker beginning `(live-claim: provisional` with or without the clause: the spec-gate step's live-claim verification, the end-to-end verification step's marker rewrite, the morning-report step's surviving-marker disposition, and the stated-conclusion paragraph's designed-provisional non-flag sentence in `skills/handover/SKILL.md`, alongside the Validate-before-proceeding step; the morning-report sentence is pinned verbatim by `tests/universal-skill-topology.test.js`, so its sweep is additive (the pinned substring stays) and the pin is updated in the same change set if the bytes move; verification: after the sweep each swept consumer sentence (the four handover sentences named above plus the Validate-before-proceeding step's) is pinned verbatim in that `lifecycleContract` list, and the extended item 6 grammar sentence in `internal/revise/spec.md` is grep-pinned by the implementation plan; the classification itself is model-owned, so it is a live claim per host, and because this entry is a grammar-owning site whose own occurrences of the marker are non-markers, its two per-host probe bullets live in the verification surface of [Operating context at shift start](features/operating-context-at-shift-start.md) beside that feature's own, probed in this batch's verification step. Lands second of the three batch entries that edit the Validate-before-proceeding paragraph, after the Reconcile entry and before [Operating context at shift start](features/operating-context-at-shift-start.md). Grammar clarification for item 6: detection is a substring match on `(live-claim: ` outside fenced code blocks, so a marker counts whether bare or backticked and whatever punctuation follows it; the only non-markers are occurrences inside the enumerated grammar-owning sites, which quote the form to define it: item 6 itself, this entry and the Reconcile entry beside it, the Validate-before-proceeding step, the stated-conclusion paragraph, the spec-gate step, the end-to-end verification step, the morning-report step, and the Provenance stamps fingerprint bullet in `skills/handover/SKILL.md`, the pre-seed sources bullet in `internal/revise/spec.md`, and the `lifecycleContract` pins in `tests/universal-skill-topology.test.js`; the validation agent reports none of those. Shipped 2026-09-03 (plugin 2.7.0): the marker grammar gained the `awaiting <precondition>` clause, `skills/handover/SKILL.md` reads a clause-carrying marker as a note and a bare one as a flag at the validate step and the morning-report step, the four existing provisional markers were rewritten to name their preconditions, and `tests/universal-skill-topology.test.js` pins the grammar.
  - **Operating context** (derived 2026-09-02 by the spec gate's grounding step from durable project knowledge; the same section governs the three entries of this batch):
    - **Deployment environment and operational criticality**: public GitHub plugin (`github.com/astenlund/nightshift`, self-hosted marketplace with autoUpdate); shipped skill prose that runs inside the author's Claude Code and Codex sessions at shift start and touches no production system or external data. Uplift `deployment_criticality` judged not fired: a failure is a stalled or mis-flagged review session, visible only to the user running it.
    - **Audience**: judgment recorded as `personal use`, matching the precedent in `.nightshift/features/present-spec-for-agreement.md`: public availability does not change the decision-maker, the engineer present at the agreement gate.
    - **Failure consequence and data or security sensitivity**: a wrong flag classification either halts an unattended run on a designed carry-forward or lets an undesigned provisional claim pass as a note; a wrong operating-context write lands calibration prose in a backlog entry. Both are recoverable through git; no external data or security-sensitive surface. Uplift `failure_consequence` judged not fired.
    - **Concurrency and compatibility risk**: the rules must hold on Claude Code and Codex hosts and across the digest contract's consumers (the agreement test fixture renderer and the revise wrappers), and the post-agreement write mutates governing backlog artifacts, so a crash between agreement and write is a designed state. Uplift `concurrency_compatibility` judged fired, matching the present-spec-for-agreement precedent for writers to governing artifacts.
    - **Reversibility and recovery cost**: git-tracked instruction prose and versioned plugin releases; a bad release is reverted or downgraded. Uplift `reversibility_recovery` judged not fired.
    - **Expected feature lifetime**: long-lived; these are permanent lifecycle rules of the handover and agreement skills. Uplift `expected_lifetime` judged fired.
    - **Derived rigor**: audience `personal use` (baseline low) plus 2 fired uplifts yields tier `high` via `node internal/revise/rigor.js "personal use" 2`; per-dimension effort: validation high, recovery high, compatibility high, observability high, proofEffort high.

- **Reconcile the Validate-before-proceeding step's general live-claim flag sentence with its carve-outs.** In `skills/handover/SKILL.md`, "they surface in the stated conclusion as a flag with the claim's context" still asserts every surviving `(live-claim: ...)` marker surfaces as a flag, while later sentences carve out `probed`, `deferred`, and designed-`provisional` markers as notes. The later-more-specific-wins pattern works but ships a contradiction each reader must arbitrate. Preferred shape: soften the general sentence to "as a flag or note per the classification below", and scope its continuation ("the user rules on them there (probe now, defer knowingly ..., or proceed leaving the flag standing)") to the flagged markers, since a note carries no ruling, so the paragraph asserts one thing. Lands first of the three batch entries that edit this paragraph, before [Make the governing spec the durable record that a provisional live-claim is a designed carry-forward](#handover-live-claim-surfacing) and [Operating context at shift start](features/operating-context-at-shift-start.md). Verification: the softened sentence and its scoped ruling continuation are pinned verbatim in the `lifecycleContract` list of `tests/universal-skill-topology.test.js` in the same change set, so the retired contradiction cannot silently return. Shipped 2026-09-03 (plugin 2.7.0): the general flag sentence in `skills/handover/SKILL.md` now defers to the classification below it, with an inclusion pin and an absence pin for the retired wording.
  - **Operating context** (derived 2026-09-02 by the spec gate's grounding step from durable project knowledge; the same section governs the three entries of this batch):
    - **Deployment environment and operational criticality**: public GitHub plugin (`github.com/astenlund/nightshift`, self-hosted marketplace with autoUpdate); shipped skill prose that runs inside the author's Claude Code and Codex sessions at shift start and touches no production system or external data. Uplift `deployment_criticality` judged not fired: a failure is a stalled or mis-flagged review session, visible only to the user running it.
    - **Audience**: judgment recorded as `personal use`, matching the precedent in `.nightshift/features/present-spec-for-agreement.md`: public availability does not change the decision-maker, the engineer present at the agreement gate.
    - **Failure consequence and data or security sensitivity**: a wrong flag classification either halts an unattended run on a designed carry-forward or lets an undesigned provisional claim pass as a note; a wrong operating-context write lands calibration prose in a backlog entry. Both are recoverable through git; no external data or security-sensitive surface. Uplift `failure_consequence` judged not fired.
    - **Concurrency and compatibility risk**: the rules must hold on Claude Code and Codex hosts and across the digest contract's consumers (the agreement test fixture renderer and the revise wrappers), and the post-agreement write mutates governing backlog artifacts, so a crash between agreement and write is a designed state. Uplift `concurrency_compatibility` judged fired, matching the present-spec-for-agreement precedent for writers to governing artifacts.
    - **Reversibility and recovery cost**: git-tracked instruction prose and versioned plugin releases; a bad release is reverted or downgraded. Uplift `reversibility_recovery` judged not fired.
    - **Expected feature lifetime**: long-lived; these are permanent lifecycle rules of the handover and agreement skills. Uplift `expected_lifetime` judged fired.
    - **Derived rigor**: audience `personal use` (baseline low) plus 2 fired uplifts yields tier `high` via `node internal/revise/rigor.js "personal use" 2`; per-dimension effort: validation high, recovery high, compatibility high, observability high, proofEffort high.
- **Init-backlog leaves rewritten files half-staged.** `relocateIndex` staged each tracked destination with its pre-rewrite blob and `updateReferences` then rewrote the working file, so migrated files showed `RM` and had to be staged again. When the reference rewrite of a relocated destination begins, the migration checks whether the destination's working tree still matches its index (`git hash-object` against the file, so filters such as autocrlf are honored), journals that decision on the move before writing the edits row, and after the rewrite restages the destination with `git add` only when that decision holds and the index entry still carries the blob the migration staged; a file with a pre-existing staged-versus-working difference keeps it with only the working tree rewritten, rewritten consumers that were not relocated stay unstaged, and the guard reads the index so interrupted reruns restage exactly once. Leaving the index alone entirely was rejected because the rename staging already existed and the asymmetry was the complaint. Fixtures cover the restage, an interrupted reference write and the preserved staged difference. Installed-host probes on 2026-09-12 (five probes across Claude Code and Codex, 466471 of a user-authorized 500000 token cap, artifacts under `.tmp/live-probes-3010`) showed Claude inverting the meaning of an inventory-time flag then named `staged`, and the following independent assessment showed that any inventory-time decision was unsafe, since an edit made to an already relocated destination during an interruption would have been swept into the index on resume; the decision therefore moved to the rewrite itself and inspect reports no flag. A fixture interrupts after the first reference write, edits the second relocated destination, resumes, and asserts the edited file keeps its original staged blob while the first is restaged. Shipped in plugin 3.0.10.
- **Init-backlog validates the backlog only after relocating everything.** `skills/ready/ready.js` now accepts a legacy `.claude` directory as a backlog root beside `.nightshift`, naming the directory it examined in its errors, and `Setup.inspect` runs it against `.claude` whenever files would move, returning the report as `backlog`; the first `apply` refuses with `backlog-validation` before the journal is written or anything moves, and both that failure and the final post-initialization failure print only `structuralErrors` and `notices` instead of the whole ready report. The journal does not persist the snapshot. Fixture: a breakout file carrying its own Requires line is reported by inspect, refused with the legacy tree intact and no journal row, and migrates after repair. Shipped in plugin 3.0.10.
- **Init-backlog hard-wrap notices demand one unwrap run per file.** An opt-in `unwrap: true` key in the options JSON makes `initialize` spawn `unwrap.js --write` over the whole `.nightshift` directory after relocation and scaffolding, return its per-file report as `unwrapped` (null when not requested) and then run the parser so the returned notices reflect the unwrapped state; the default stays non-destructive, and the skill text names both routes, the option or one multi-target unwrap invocation, instead of a per-file loop. Collapsing the parser notices was rejected because the per-notice count and first line remain useful evidence and the unwrap CLI already accepts a directory. The same installed-host probes showed Codex running `unwrap.js` without `--write`, a report-only run, because the skill text had not named the flag, so the text now names it; both hosts otherwise refused apply until the structural repair, ran one multi-target unwrap, kept a preserved staged/working difference out of a broad add, and Claude preserved the journal on a genuine migration-drift failure. Shipped in plugin 3.0.10.

### Acknowledge accepted handovers clearly

Proposed by the user during retained-release delivery in this repository, run `2a89bde5-9c0c-4ba5-b67f-dadc545bfcc4`, Codex session `01a09c60-dd6e-7d42-b8a0-7333fd211d92`: a clear "Handover accepted" message would tell the user when they can leave the keyboard. The idea was preserved as the `clear-handover-acceptance` run follow-up, and the user chose to track it on 2026-09-15.

Make acceptance of an explicit handover unmistakable once the agreed scope and essential continuation prerequisites are settled. Briefly carry forward the accepted work and relevant limits without reopening settled requirements or bundling publication authority into the acknowledgment. If a material decision or continuation prerequisite remains unresolved, identify it instead of claiming acceptance. Verify the model-owned acknowledgment on both supported hosts for new runs, existing runs and unresolved prerequisites. This is distinct from [switching an existing run to unattended mode](BUGS_HISTORY.md#handover-leaves-existing-runs-in-attended-mode) and [delivering the morning report](BUGS_HISTORY.md#handover-omits-the-morning-report). Tracking does not authorize implementation.

Shipped in plugin 3.2.0 with the handover transition. `skills/handover/SKILL.md` has the controller acknowledge only after the `handover` operation returns: it says the handover is accepted and the user can leave, restates the work and limits briefly, says whether the run operates unattended and that a morning report will be waiting in the session, mentions publication only to restate authority already granted, and names an outstanding decision, readback or refused admission with its recovery instead of claiming acceptance. What it says about a run that stayed attended depends on which prerequisite failed. Both installed hosts produced the acknowledgment after the recorded operation without a repeated readback, as [the acceptance report](reports/handover-transition-and-morning-report-20260919.md) records.

### Paused strong gate pauses repair application

Retrospective finding from run `c675a074-6431-46e2-85b7-e3b8616e9220`, confirmed by two independent assessments of the proposal: a repair batch was applied and left unreviewed while the strong reviewer was rate-limited, although an advisory reviewer was available and cross-host strong substitution was the prescribed path. The operating brief required strong assessment after repairs but left room to interpret a paused gate as permitting more repair application.

Implemented for candidate plugin 3.2.1 in run `45cf8a48-096b-4e2b-9425-76541b971dbd`. Both Model roles and ownership and Review and repair in [the operating brief](../internal/workflow.md) now require securing a permitted strong substitute before further repairs to affected work, or pausing those repairs while independent authorized work continues. Advisory assessment never supplies required strong review coverage. Explicit model requirements and qualified same-host review remain governed by the existing rules. [The verification report](reports/paused-strong-gate-20260921.md) records five controlled decision checkpoints on each native host, exact evidence hashes and the limits of those observations; these probes do not establish end-to-end recovery from an actual provider outage. Publication remains a separate user decision.
- **Continue agreed Ready work in the current session** (`skills/ready/SKILL.md`): shipped on 2026-09-24 in the 3.2.4 candidate. Once the selection and readback are agreed the agent proceeds with the agreed work in the current session and does not ask whether to hand over, start a separate task or open a new conversation; the user directs handover or any other change of execution context. Installed-host evidence showed in-session continuation after an agreed readback and an honored explicit handover on both Claude and Codex. See [the acceptance report](reports/ready-selection-boundary-20260924.md). Original entry: Reported from a `/ready` run in this repository on 2026-09-11. After the user picked two entries and the agent produced an agreed readback, the agent closed by asking whether to hand the selection to Nightshift or implement it in the session. The user's direction is that a selection plus agreed readback is the go-ahead for direct implementation in the session, and that a handover happens only when the user says so; asking which is ceremony that costs a turn. The [separate-task incident](reports/inbox-triage-20260921.md#ready-selection-prompts-an-unnecessary-separate-task-choice) recurred in this repository on 2026-09-21 with bound version 3.2.0. After selection and scope readback, the controller asked whether work should continue in the current task or a separate task. The user had requested no new task; the question caused an unnecessary clarification exchange. No separate task was created. The controller's account that it confused execution context with a new conversation is a hypothesis, not an independently established cause. Revise `skills/ready/SKILL.md` so that once a selection and scope readback are agreed the agent proceeds to implementation in the current session unless the user directs otherwise, and keep the readback itself free of unsolicited handover or separate-task questions. Selecting an item still starts investigation and readback; agreement on scope and explicit handover remain distinct decisions. The behavior is model-owned, so verify ordinary in-session continuation and explicit user-directed handover or new-task cases with installed-host evidence.
- **Documentation edits after review import stale the review before advance** (`internal/runtime/lifecycle.js`, `internal/runtime/REFERENCE.md`, `internal/workflow.md`): shipped on 2026-09-24 in the 3.2.5 candidate, run `46fc13f1-98fc-4a3e-953c-958a31261ae4`. The review gate now separates snapshot freshness from its other conditions, so when the latest assessment's inputs changed after import both gated advances (review to documentation, documentation to complete) keep `review-required` but say the assessment is stale and that a current cumulative assessment covering the changes is needed; other gate failures keep their messages. The reference and the brief's documentation paragraph now say to land backlog and documentation edits after the task advances to documentation, since they need a current cumulative assessment before completion either way. The stale message deliberately does not offer restoring the changed inputs as an alternative, because after a repair restoration alone cannot satisfy the gate. Regression coverage is in `tests/runtime-regressions.test.js`. Original entry: Observed on 2026-09-24 in run `0a2dceee-1692-41f6-86bc-bc5c7b272a61`. After importing a clean code review, the controller wrote the acceptance report and moved backlog entries, then called `advance` to documentation; it was refused with the generic `review-required` message because the edits made the imported review stale. The reference said "Land backlog and documentation edits before dispatch or after import", which read as permitting that order. The same class of slip had occurred once before on 2026-09-18. Recovered with a path-scoped stash, advance and pop.
- **Runtime checks cannot launch a bare executable name** (`internal/runtime/operations.js`, `internal/runtime/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.5 candidate, run `46fc13f1-98fc-4a3e-953c-958a31261ae4`. A check executable with no path separator now resolves before reservation to an absolute path through `resolveTrustedExecutable` over the check environment's PATH, excluding the project and implying `.exe` on Windows, and the resolved path is recorded as the check's executable; an unresolvable name fails with `executable-not-found` without reserving anything, and explicit paths run unchanged. Assessor probes share the same resolution at launch, inside their already reserved operation, so an unresolvable probe executable fails with `executable-not-found` and leaves that operation worker unverified; 3.2.6 moved that resolution before reservation (**Unresolvable probe executables leave an unverified worker**, below). Coverage in `tests/runtime-adoption.test.js` includes a real contained Windows launch of a bare name. Original entry: Observed on 2026-09-24 in run `0a2dceee-1692-41f6-86bc-bc5c7b272a61`. A runtime `check` with executable `node` failed with "Windows job could not start the host: spawn" and left a pending reserved attempt with no collected result, because the contained launcher performs no PATH search; the same check with the absolute path to `node.exe` succeeded, and the reference did not state the requirement.
- **Attended runs show a Stop message after every reply** (`internal/runtime/hook.js`, `internal/runtime/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.6 candidate, run `0c326496-62f7-4eb1-a9e0-1a7a412933bf`. Stop now returns nothing for a running attended run without a handover. It used to return the `systemMessage` "Nightshift attended work remains saved. Reconcile its outstanding obligations before resuming or claiming completion." after every reply: an instruction meant for the controller that blocked nothing and carried no decision for the user. The other `systemMessage` returns in the hook were reviewed and kept. The two pause messages ("paused on user decisions" and "unfinished blocked work") fire only for a handed-over or unattended run when Stop deliberately lets the turn end, and tell a returning user the run is paused rather than complete. The PreCompact reminder stays because [the handover spec](specs/handover-transition-and-morning-report.md) retains it, and it fires once per compaction rather than after every reply. `tests/runtime-hooks.test.js` asserts the empty response. Whether Codex displayed the removed message was never observed. Original entry: raised by the user on 2026-09-24 during attended run `46fc13f1-98fc-4a3e-953c-958a31261ae4`, asking whether the "Stop says" messages were needed in the chat.
- **Unresolvable probe executables leave an unverified worker** (`internal/runtime/cli.js`, `internal/runtime/probes.js`, `internal/runtime/operations.js`, `internal/runtime/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.6 candidate, run `0c326496-62f7-4eb1-a9e0-1a7a412933bf`. The probe action now validates the assessor's command and resolves its executable before reserving a worker. It uses `prepareProbe`, which shares `prepareCommand` with checks and resolves against the canonical root, which also excludes the private copy beneath it. An unresolvable executable is therefore refused with `executable-not-found` and leaves no worker. The fix also covers a sibling with the same consequence: any refusal inside the reserved operation before the contained launch, such as a non-ASCII fixture file or a failed private Git boundary, left the worker `unverified` although nothing ran. `reservedOperation` now records whether a launch was attempted and marks the worker `failed` when it was not; only an attempted launch without termination evidence stays `unverified`. A mutation probe that restored the old classifier made the new fixture in `tests/runtime-probes.test.js` fail. The fixture could not use `stale-probe` for this, because receipt reading refuses a stale receipt before any reservation; `stale-probe` still reaches the reserved operation when a canonical edit lands after receipt reading, and the classifier marks that worker `failed` too. Original entry: traced by cumulative review `7a3f4c82-512f-4f7c-9691-b0d6507edc65` in run `46fc13f1-98fc-4a3e-953c-958a31261ae4` and confirmed by a fresh skeptic.
- **Probe proposals with implausible timeouts run as specified** (`internal/runtime/review.js`, `internal/runtime/probes.js`, `internal/runtime/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.6 candidate, run `0c326496-62f7-4eb1-a9e0-1a7a412933bf`. Between 2026-09-12 and 2026-09-24, independent assessors proposed probe `timeoutMs` values that all read as seconds for suites taking about a minute. The four occurrences were 600 in an unattended run, sub-second values from two assessors in run `c675a074-6431-46e2-85b7-e3b8616e9220`, 180 in run `d4a44daa-96be-4ac4-bcaa-d16d8596584f` and 240 in run `46fc13f1-98fc-4a3e-953c-958a31261ae4`. Each cost a re-dispatch, and the last incomplete attempt alone cost 572,891 tokens. The root cause was the reviewer brief, which asked for a "timeout" without a unit, while the schema's range of 1 to 120000 also accepted seconds. The brief and the schema description now state that `timeoutMs` is in milliseconds, from 5000 to 120000, and the schema minimum is 5000. The probe action refuses a proposal outside that range with `invalid-probe-timeout` before reserving a worker, judging the assessor's value before the run deadline shortens it. No controller override was added, so probes still run exactly as the assessor specified them. The producer-schema fixture in `tests/runtime-review.test.js` shows a real JSON Schema validator rejecting 240. The user chose deterministic evidence only, so how real reviewers respond to the new wording remains unverified until a later run shows it.
- **Dispatch timeout applies per attempt while the launcher bound is total** (`internal/releases/service.js`, `internal/releases/processes.js`, `internal/runtime/limits.js`, `internal/runtime/cli.js`, `internal/runtime/review.js`, `internal/runtime/operations.js`, `internal/runtime/REFERENCE.md`, `internal/releases/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.7 candidate, run `bf8200ce-157c-416f-98a0-fd4016ebfc35`. Dispatch `timeoutMs` stays per attempt, now documented, so a fallback gets its own full timeout. The launcher now passes its operation deadline to the runtime as `operationDeadlineUtc` in the binding context; the deadline is the envelope `timeoutMs`, or the one-hour default and maximum now shared as `MAX_OPERATION_TIMEOUT_MS`. Before reserving a worker, dispatch refuses with `operation-time-limit` unless all candidate attempts, capped by any run deadline, fit before that deadline less a 60-second exit margin, and the refusal names the time needed. Every dispatch attempt, check and probe is also capped at that point as it launches, so a runtime timeout records its evidence before the launcher ends the process. The first cumulative assessment asked whether check and probe timeouts computed before preparation could overrun that point; tracing showed they could, because snapshots and private copies ran after the relative timeout was fixed, so `reservedOperation` now recomputes the bound at launch, with deterministic tests adapted from the assessor's proposed probe. Without an operation deadline (the development CLI, or an older launcher that passes none) behavior is unchanged. Original entry: Observed on 2026-09-24 in this repository, run `0c326496-62f7-4eb1-a9e0-1a7a412933bf`, dispatch `0fc72873-32d2-4ae1-ae4a-c678ca5d4c47`. `internal/runtime/review.js` gives each candidate attempt `Math.min(timeoutMs, remaining)`, and without a run deadline `remaining` is the full `timeoutMs` again, so a dispatch with a fallback can run for the number of candidates times `timeoutMs`. The retained launcher separately bounds the whole guarded operation (the envelope `timeoutMs`, at most one hour). The controller set that bound to 1500000 ms with a review `timeoutMs` of 1200000 and two candidates. The Codex Astra attempt looped until its 20-minute timeout (see the bug "Dispatch does not detect a reviewer stuck in an output loop"), and the Fable fallback was composing its final report when the launcher ended the dispatch helper at 19:43:15Z. No receipt was written, the reviewer worker stayed `running` with its runner gone, and it was reconciled by hand with `wait` (`runner-missing`) and `worker-finished`. A re-dispatch with a 3000000 ms bound completed. `internal/runtime/REFERENCE.md` does not say that dispatch `timeoutMs` applies per attempt, and nothing relates it to the launcher bound. The user chose to track this at triage on 2026-09-24.
- **Runtime CLI rejects an unrecognized request action with a clear error** (`internal/runtime/actions.js`, `internal/runtime/cli.js`, `internal/runtime/lifecycle.js`, `internal/releases/service.js`, `internal/runtime/REFERENCE.md`): shipped on 2026-09-24 in the 3.2.7 candidate, run `bf8200ce-157c-416f-98a0-fd4016ebfc35`. One accepted-action list in `internal/runtime/actions.js` now serves the runtime CLI and the launcher's runtime entry. A request with a missing or unknown `action` is refused before any store, state or ownership check, with a message naming the `action` key and listing the accepted actions. The runtime reports `invalid-request`; the launcher keeps its existing `invalid-runtime-request` code for the same refusal and launches nothing. The lifecycle `transition` default now uses the same `invalid-request` code and message instead of `unknown-action`, and a test keeps every lifecycle transition in the accepted list. The runtime reference now states the request shape. Original entry: Observed in this repository on 2026-09-11. `internal/runtime/REFERENCE.md` names the operations (`status`, `inspect`, `create` and so on) but never states the request key, and `internal/runtime/cli.js` dispatches on `request.action`. A request written as `{"operation": "status"}` fell through every branch to the owner check and failed with `stale-owner`, a message about controller identity, on a read-only status call; the controller spent two calls and a read of the CLI source to find the cause.
- **Windows job start failures omit the executable and the Windows error** (`internal/runtime/windows-job-runner.ps1`, `internal/runtime/windows-job.js`, `internal/runtime/hosts.js`, `internal/runtime/REFERENCE.md`, `tests/runtime-hosts.test.js`): shipped on 2026-09-25 in the 3.2.8 candidate, run `ff195382-31a4-4fea-bb48-ca56d20507bb`. Observed on 2026-09-24 in run `bf8200ce-157c-416f-98a0-fd4016ebfc35`, where an assessor's probe failed with only "Windows job could not start the host: spawn" and finding the cause needed a check of the bound bundle's version. Every spawn-failed return in the runner now carries its stage (`command`, `setup`, `create-process`, `job-assignment` or `resume`), and a `create-process` failure also carries the Win32 error captured with `Marshal.GetLastWin32Error()` directly after `CreateProcessW`, plus its system message. The frame is serialized with ASCII-only JSON escaping because the system message can be localized, and a zero error code is omitted rather than reported as success. `startFailure` in `windows-job.js` validates the exact key set, the stage, a positive integer code and a message of at most 1024 characters; the runner truncates longer system messages (some exceed 2000 characters) rather than lose the frame, and the JS side fills a message's `%1` insert with the executable. A malformed frame fails as invalid and, unlike before, no longer marks the job empty. The error names the executable, the stage and any Windows error, for example `... could not start the host C:\x\missing.exe (create-process): Windows error 2: The system cannot find the file specified.`; the termination-unproven failure also names the executable. Checks and probes already record the thrown message as their operation worker's failure evidence. Reviewer attempts lost it on both hosts: Codex threw a generic `host-start-failed` message, while Claude returned a failed result whose detail stayed in the attempt's `result.json` and the dispatcher recorded only "Host did not return an attributable completed assessment". Both hosts now throw `host-start-failed` carrying the detail and the job's cleanup evidence, so the attempt error names the cause; when cleanup is unproven, both instead fail closed with `termination-unverified`, whose message keeps the host failure as its cause, through one shared guard so the hosts cannot diverge. Win32 codes for the `setup`, `job-assignment` and `resume` stages were left out by agreement, since capturing them would touch many more Windows calls. A Windows-only test starts a nonexistent absolute path through the job directly, through both host paths and through the contained check and probe path, and a platform-independent test pins the frame grammar. Independent Astra assessment `890e1dd4-b514-43cd-b9a3-90ea8963e1ae` found no defects after its execution probes; the nested `tests/runtime-probes.test.js` cases failed only at private-copy depth, the known [path-depth limitation](#private-review-copies-fail-at-windows-path-depth), and passed 9 of 9 at checkout depth.
- **Wait deadline test depends on fixture setup time** (`tests/runtime.test.js`): shipped on 2026-09-25 with the 3.2.8 candidate, run `ff195382-31a4-4fea-bb48-ca56d20507bb`. In run `bf8200ce-157c-416f-98a0-fd4016ebfc35` the recorded runtime suite failed once (188 of 189) because "wait ends at the run deadline instead of the requested timeout" computed its 400 ms deadline before creating the fixture; under the concurrent suite, setup outlasted the deadline and registering the worker was refused with `resource-limit` before `wait` ran. The test now creates the fixture without limits, registers the worker, and only then sets the deadline through a direct fixture-state update, keeping its assertions. A slow machine that spends the whole 400 ms before the first `wait` still yields `deadline`, so setup time can no longer fail the test. Test-only, so it carries no version increase of its own.
- **Ready reports broken backlog links, title drift and unreachable records** (`internal/backlog-links.js`, `skills/ready/ready.js`, `internal/backlog-catalog.js`): shipped on 2026-09-25 in the 3.2.9 candidate, run `6e70931a-3760-46e2-b628-fdaa4a29f0e3`, commit `fff7239`. Ready now emits three notices, never structural errors, computed by one pure module that the CLI and the catalog adapter share: links and heading anchors from the active indexes and from records under `features/`, `bugs/` and `patterns/` that no longer resolve inside the backlog (history files are link targets and reachability roots, not sources; links into reports, specs, migration material and code lie outside what either front end reads and stay with the [whole-backlog coherence audit](features/backlog-coherence-audit.md)); title drift, the settled meaning of excerpt drift, between an active entry heading's inline record link and that record's H1; and one aggregated notice for records that no chain of links from any index reaches. Links that existing checks already report (dependency declarations, including wrapped continuations and references used only there, and entry headings linking a missing record) are skipped. Negative knowledge: the check is a hand-rolled model of the Markdown subset backlogs use, since the plugin takes no package dependencies, and five successive complete assessments each found further block-boundary cases (setext and entity headings, indented and quoted code, reference definitions, HTML comments, escapes, paired emphasis, list containers). The module header now declares the supported subset and its unmodeled constructs, so later reports can be judged against a stated boundary instead of being patched one construct at a time. Block structure reuses the unwrap scanner's model (`describeBlocks`), refactored without changing unwrap behavior; list-item context exists only on the link side. Deferred: a fence opened on a list item's marker line, which the shared model also misreads for unwrap. Accepted: a reference-style entry heading gets no title comparison, matching the parser's inline-only entry grammar.

### Private review copies fail at Windows path depth

Fixed on 2026-09-26 in the 3.2.10 candidate, run `36aad5de-2825-4818-ba8c-e1ea8c8e72a4`. Review workspaces and probe copies are now created by `createPrivateCopyDirectory` in `internal/runtime/copies.js` directly under `.nightshift/runs/c/<eight hex digits>`, instead of under the dispatch directory. A probe copy now sits 27 characters below the project root instead of 113, and a review copy's `project` directory 35 instead of 79, so a probe level saves 86 characters and a review level 44, short of the readback's estimate of about 80 per level, which the 79-character review prefix could never reach. Requests, attempts, receipts and probe results stay under the dispatch directory, which also keeps a copy of the cumulative diff and manifest the reviewer read, and `request.json` records the review copy as `workspace` and a probe's `result.json` records its copy as `copy`. Nothing is skipped: a nested probe that cannot establish its Git boundary still fails. Acceptance ran `tests/setup.test.js`, `tests/runtime-probes.test.js` and `tests/runtime-review.test.js` from a copy of this checkout placed at the new probe depth: 40 of 40, 9 of 9 and 53 of 53 passed. As a control, the same suites at the old probe depth with the previous runtime failed 1 of 40 (the same migrated-run case as below), 6 of 9 and 43 of 52. Deterministic tests in `tests/runtime-probes.test.js` and `tests/runtime-review.test.js` pin the layout.

Observed throughout run `c675a074-6431-46e2-85b7-e3b8616e9220` on 2026-09-15 to 17. Every deterministic suite executed as a reviewer probe inside a private review copy failed exactly one case: the recovery suite's nested probe-inside-probe Git initialization refused with `probe-git-boundary` at that depth, and in one run the EOL autocrlf case failed with `Filename too long`. Both pass at ordinary checkout depth, which the recorded check `probe-isolation-at-supported-checkout-depth` exists to prove, so every strong reviewer had to be told the failure was environmental. The acceptance report already records process-only Git long-path support as the workaround for the EOL case.

Make private copies survive their own depth: shorten the copy path under the review directory, or enable process-only long-path support for the copy's Git operations, and make the nested-probe recovery case skip with a stated reason when it cannot establish a Git boundary rather than fail. Runtime code, so it ships with a version increase. Tracking does not authorize implementation.

Recurred in run `d4a44daa-96be-4ac4-bcaa-d16d8596584f` on 2026-09-18 to 19, the handover transition and morning report delivery, which spent 14,869,844 review tokens across 18 dispatches, summed from that run's review receipts, and 14,445,999 live-verification tokens on a change of roughly 400 lines, with four already tracked defects firing during it. Here, a requested probe of `tests/runtime-review.test.js` and `tests/runtime-probes.test.js` failed 39 cases with git's `Filename too long`, because those suites nest review copies inside an already deep probe copy, and the assessor had to be given the environmental explanation to weigh.

The run-adoption delivery on 2026-09-21 to 22 added a controlled comparison: the same saved source passed all three probe cases at a short project path and failed all three at the original deeper layout. Failing Git working directories were 272 or 280 characters; `spawnSync` returned null status, `ENOENT` and no output before Git started, despite process-local `core.longpaths=true`. Baseline and candidate also matched in separate shorter/deeper comparisons. Verification recovered through a shorter private copy. The exact Windows/process-launch cause and broader impact remain unknown. Distinguish this launch failure from the earlier Git-level error: Git configuration cannot repair a process that never starts, and a skipped or unavailable probe is not successful acceptance evidence. The user chose to track this recurrence; [triage evidence](reports/adoption-session-triage-20260922.md#deep-private-probe-paths) preserves its limits.

Recurred on 2026-09-25 in run `ff195382-31a4-4fea-bb48-ca56d20507bb`: an assessor probe running `tests/runtime-probes.test.js` inside a private copy failed six cases with "Could not establish an independent Git repository for the probe" (review `a108b64d-72ab-4e40-afb5-9bfe91da179c`), while the same file passed 9 of 9 at checkout depth as a recorded check, which the assessor accepted as the environmental explanation. The user chose to track this recurrence at triage.

Recurred again later on 2026-09-25 in run `6e70931a-3760-46e2-b628-fdaa4a29f0e3`: all 11 probes of `tests/setup.test.js` in private review copies failed one case, "a migrated stopped run resumes default spec assessment with current paths and original history", with git's `Filename too long`, while the recorded `setup-package-gate-tests` check passed in the canonical checkout. Every assessment in that run had to qualify its setup evidence as environmental. The user chose to add this recurrence at triage.
