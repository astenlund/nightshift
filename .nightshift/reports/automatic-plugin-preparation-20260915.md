# Automatic plugin preparation: acceptance evidence

**User-directed stop, since resumed:** Work was paused at the user's request because tokens were running low, and resumed on 2026-09-17. The independently confirmed defect this notice originally recorded, that Claude project and local `disableAllHooks` settings were not respected by Nightshift admission, has since been repaired; see the resumption section at the end of this report. Earlier clean assessments and passing checks do not establish completion of that boundary, and the cumulative assessment covering the repair has not yet run. Final verification and lifecycle closing remain incomplete; changes are uncommitted and unpublished.

## Status and scope

The 3.1.1 implementation had a clean cumulative code assessment when this report was written, before the hook-disabling defect was found and repaired; the reassessment covering that repair is outstanding. Its deterministic checks pass on current inputs. The requested model-owned behavior has now been observed on both hosts, including Opus with an original 3.1.0 binding. Its final response was captured before the harness interrupted Stop processing, so native process completion and final usage retain the qualifications below. This is a local candidate, not a published release.

The [governing design](../specs/automatic-plugin-preparation.md) records the agreed outcome. Skill invocation prepares Nightshift automatically. Ready and Exploring use retained resources without requiring continuation activation. A narrow administrative compatibility path applies that admission policy while running an existing task's original retained parser. Runtime and hook implementations stay on their original release. Preparation preserves deliberate disabling/removal, native hook definitions, generation and trust.

## Implementation and independent assurance

The shared resource interface now directs all eight skills through automatic preparation. Preparation reconciles interrupted registration before considering fresh setup, including matching no-op writes without a journal. Administrative bootstrap publication is separate from native hook configuration. The current bootstrap routes Ready admission through the current verified administrative bundle while dispatching the parser from the task's original binding.

Independent assessment found and freshly validated interrupted-first-registration recovery, old-session administrative routing, contradictory activation prose, the old-binding admission boundary, and recovery rewriting already-applied hooks. Those findings were repaired with cumulative reassessment. The final governing-spec assessment `a51e0ab0-219c-402f-a70e-456619d8c0f2` and cumulative implementation assessment `4f175b78-6a05-43ff-b9c8-1004d190b430` returned no findings. The implementation assessment explicitly does not certify remaining installed-host acceptance or publication.

Every recorded named check passes on current inputs, which the runtime verifies mechanically at task and run acceptance rather than from a count quoted here: release, recovery and host behaviour; packaging and byte integrity; the focused probe-isolation case at the ordinary checkout depth; the preparation repair regressions; and the native reporting guard. Case counts are deliberately not pinned, because the suites grew with the hook-disabling repair. The tests include original 3.1.0 bootstrap bytes, actual bound parser execution, administrative versus bound routing, interrupted writes and no-ops, and rejection of dependent operations without activation.

Independent private probes initially encountered Windows path-depth failures. Process-only Git long-path support resolved the EOL case. The nested probe-inside-probe Git initialization remained unavailable at that depth; its exact isolation case passed from the normal checkout using its own private fixtures. These are distinguished from the independently reproduced hook-rewrite defect. No global Git configuration changed.

## Native evidence and qualifications

The environment was Windows, Node 22.23.2, Codex CLI 0.154.0, Claude Code 2.1.268 and PowerShell 7.6.6. Real Claude model calls used `claude-opus-5`; independent assessments used Astra because the current review gate cannot admit Opus and Fable was unavailable.

| Boundary | Codex | Claude |
| --- | --- | --- |
| First-use Ready without a setup conversation | Observed on an earlier 3.1.1 candidate | Observed on an earlier candidate; temporary-file wording also recurred in the final check despite subsequent guidance |
| Ready, Exploring and repeat Ready without activation | Three successful bound parser runs, unchanged binding, zero activation | Three successful bound parser runs and final Ready/Exploring/repeat report observed; original binding and zero activation preserved |
| Dependent operation still requires continuation | init-backlog inspection blocked as expected; its technical wording prompted clearer shared guidance | init-backlog was rejected for missing activation; the final response gave a reopen-and-retry action |
| Ready and Exploring with an existing 3.1.0 binding | Two successful parser runs; original identity, hook definitions and generation preserved; zero activation | Three successful parser runs; original identity, definitions and generation preserved with zero activation |
| Deliberate disabling/removal and unrelated configuration | Native zero-inference controls passed | Native zero-inference controls passed |
| Interrupted applied write and no-op across administrative upgrade | Both native zero-inference cases passed, preserving configured trust | Both native zero-inference cases passed, preserving configuration bytes and generation |

The existing-binding cases used explicitly seeded historical bindings for the actual native test sessions and original 3.1.0 resources. They do not claim that the models themselves originally created those bindings. The final Opus response still said it had deleted its temporary request files. This is a minor reporting deviation from the shared silence instruction. Its derived disclosure flag has been corrected to true; this response is not evidence of fully silent preparation. Functional parser, binding-preservation and activation-boundary observations remain valid. A final-answer check was then added to the shared policy. A native Opus reporting-only continuation read that updated guidance and produced a complete result without preparation or cleanup disclosure. Its 59,460-token result and a mutation control are recorded in `live-34a1d81c-06e7-495c-92dc-69836748dd8d/reporting-evidence.json` under the same fixture. This is targeted validation of the changed reporting step using completed operation history; it does not replay the earlier operations or assert current activation after reopening. The original failing response remains preserved. Native controls are code-owned integration evidence; they do not replace model-owned interaction checks. Earlier candidate observations remain identified as such, rather than being relabeled as execution of the final payload.

Raw transcripts, source bundles, scoped tool decisions, usage records and derived behavior checks remain under `.tmp/auto-prep-live`. The original-version reproduction is preserved under `.tmp/auto-prep-proof-180fbadf-aec1-4ea3-a853-fb584e98c70d`. Runtime review receipts and private probes belong to run `c675a074-6431-46e2-85b7-e3b8616e9220`.

## Budget and unfinished obligations

The user authorized 4,000,000 aggregate live-testing tokens, separate from ordinary implementation and reviews. The proposed 5M increase was unnecessary and withdrawn: the existing reservation fitted the original allowance. The cap remains 4M. Two earlier interrupted attempts still retain 750,000-token uncertainty allowances each.

The final Opus check produced the expected response and entered Stop processing before the harness threshold interrupted it. Its native transcript finalizes 16 preceding messages at 344,731 tokens. The final streamed response reports 30,537 input tokens and 798 output tokens. Independently assessed conservative accounting uses 376,066 observed tokens for this attempt and reserves another 63,202, the unused portion of the native model's 64,000-output-token limit. All tool approvals were answered, no fixture runtime run existed, and descendant termination was observed. The no-run Stop handlers contain no model dispatch. Final CLI-result and successful Stop-completion claims are intentionally not made.

After the reporting-only check, the reconciled aggregate is 2,100,125 observed tokens plus 1,563,202 reserved uncertainty, totaling 3,663,327 against 4M. Independent assessment `278205fb-9f08-4d44-a52a-67dc497a8807` accepted the earlier 3,603,867 bound. The separate finalized reporting result adds 59,460 tokens; the ledger records their 3,663,327 total. Raw records and the proposal are preserved in the final Opus fixture, `claude-0295f259-dbde-40f6-a1ed-dc9bf7f92720/live-d810fdc1-ff5e-4c68-b16a-a3ab45190af4`.

The user explicitly handed over. A native persistent goal carries the queue, but the existing runtime cannot switch an already-created attended run to unattended mode. This development run therefore does not claim verified retained Stop-hook protection. That limitation is already tracked separately.

Spec and implementation assessment ran. The bounded accounting has independent assessment; final report assurance and lifecycle closing are recorded in the run. Completion is established through the run's actual documentation, retrospective and triage records, not from this report's presence. Publication remains unapproved.

## Resumption on 2026-09-17

Run `c675a074-6431-46e2-85b7-e3b8616e9220` was adopted by a Claude controller after the original Codex session became unavailable, and resumed under explicit user authority. The runtime records the previous owner, the adopting identity and that authority; the transfer used the store's own transaction and is in the run history.

The confirmed finding `5755e869-5b4a-4a4d-b110-93cf05b1b4ef:R1` is repaired. Claude hook disabling is now taken from the host's own effective settings through its `get_settings` control request rather than from the user profile's `settings.json`, so managed, command-line, project local, shared project and user layers all apply in their documented precedence. The resolution was established against a live host before implementation across six cases: project disable, project local disable, user disable, user disable with project re-enable, project disable with local re-enable, and nothing set. All six matched the documented precedence, including both re-enable directions, which a combined reading of the individual files would decide wrongly.

The repair covers registration, the Ready view and dependent admission. An initial pass covered only registration and Ready, which left the two paths resolving different directories inside one session; independent assessment identified that gap and it was closed by resolving every admission path against the same project. One operation resolves the Claude effective value once. An inspection that cannot run remains a distinct recoverable failure rather than an enabled result.

Deterministic coverage was added for effective-settings admission, project-scoped resolution from a profile-based inspection, unavailable inspection, automatic registration refusing a deliberate opt-out, dependent admission sharing the Ready view's project, and single resolution per operation. The repaired admission, registration, Ready-view and bootstrap-routing sites were probed for vacuity by restoring their pre-repair semantics and confirming the covering cases fail, with the source restored byte-identically and verified by digest; the hook path's working-directory fallback was not among them until a strong assessment noted the gap, and is now covered by its own case. The recorded named checks were re-run on current inputs and pass.

Two host behaviours the repair depends on were then established by execution under the strong assessments, through the runtime's probe mechanism against Claude Code 2.1.274 with isolated profile and project directories. The nested safe-mode session that resolves effective settings fires no hooks: with SessionStart, PreCompact and Stop marker hooks registered in the profile and a SessionStart marker hook in the project, and with `CLAUDECODE=1` inherited exactly as inside a Claude hook or tool child, `initialize` and `get_settings` both succeeded in under half a second and no marker hook ran, so an inspection neither re-enters the Nightshift bootstrap nor writes an activation record nor executes project hooks. Its filesystem footprint is one-time: an empty profile gains the CLI's own `.claude.json`, one backup and `sessions/` on first use, and further inspections against the same project, a different project or the profile directory itself add and change nothing, create no `projects` entry, and leave the real home configuration untouched. The precedence evidence was reproduced the same way: user false with project true resolves true, user true with project false resolves false, and nothing set leaves the key absent. Those probe records live under the run's review directories and were re-run for each assessed snapshot.

Independent assurance for this repair is incomplete. The supported strong reviewers are `claude-fable-5-1` and `gpt-6-astra`; the Codex allowance carrying Astra was unavailable, and a dispatch attempting Fable was refused by the host for exceeding its limit, preserved as a failed attempt under the run's review records. A fresh Opus pass was used as an advisory review only and is deliberately not recorded as the cumulative assessment. Its confirmed findings were repaired; its remaining observations are recorded as follow-ups. The user's standing requirement is that a supported strong reviewer assess this change before any publication.
