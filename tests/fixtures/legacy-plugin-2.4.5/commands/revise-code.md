---
description: "Use when a code change (diff, staged work, or named files) is ready for deep multi-agent review before it ships."
---

# revise-code

Invoke the `nightshift:revise` skill with artifact type `code` and scope `$ARGUMENTS`. This command is the entry point; the skill is the shared engine that owns the entire procedure.
