---
description: "Use when an implementation plan has been written and needs hardening before execution begins."
---

# revise-plan

Invoke the `nightshift:revise` skill with artifact type `plan` and scope `$ARGUMENTS`. This command is the entry point; the skill is the shared engine that owns the entire procedure.
